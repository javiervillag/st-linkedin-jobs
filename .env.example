# LinkedIn Daily ST Discovery
# Copy to .env and fill in values

# DeepSeek API key (required for AI classification)
DEEPSEEK_API_KEY=sk-bd8d4eb71f5c409c801fb1f680421b91

# How many days back to scrape (default: 1)
DAYS=1

# Paths (defaults work for Railway)
DB_PATH=data/linkedin_companies.json
DATA_DIR=data

# Set to "true" to skip API calls (test mode)
# DRY_RUN=false
